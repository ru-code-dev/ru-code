.artifacts/
test-results/

# ru-code: per-suite run artifacts (evidence, playwright results, fake-CLI logs) —
# disposable, regenerated on every run; nothing under these dirs is tracked.
.artifacts-*
